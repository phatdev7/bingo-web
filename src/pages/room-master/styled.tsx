import styled from 'styled-components';

const Main = styled('div')`
  margin: 30px 0;
`;

const Content = styled('div')``;

const FormDiv = styled('div')`
  display: flex;
  flex-direction: column;
`;

export { Main, Content, FormDiv };
